# from git import Repo
# import os

# join = os.path.join

# class GitManager(object):
#     def __init__(self):
#         rw_dir = "x"
#         repo = Repo.repo(rw_dir)

#     def clone(repo,repo_name):
#         target_dir = "workspace/git/caas_base"
#         Repo.clone_from(repo,join(target_dir,repo_name),branch=master)